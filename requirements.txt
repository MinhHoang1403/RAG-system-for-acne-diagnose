# =============================================================================
# requirements.txt – Acne Agent System
# Pin ALL transitive dependencies in production (use pip-compile / uv lock).
# This file lists direct dependencies grouped by layer.
# =============================================================================

# ---------------------------------------------------------------------------
# Core / Config  (12-Factor: config)
# ---------------------------------------------------------------------------
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ---------------------------------------------------------------------------
# API Layer  (src/api)
# ---------------------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9       # file uploads
starlette>=0.37.0

# ---------------------------------------------------------------------------
# Agent Layer  (src/agent)
# ---------------------------------------------------------------------------
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-google-genai>=1.0.0
langchain-ollama>=0.1.0
langgraph>=0.1.0
openai>=1.30.0
anthropic>=0.28.0
google-generativeai>=0.7.0

# ---------------------------------------------------------------------------
# Skills Layer  (src/skills)
# ---------------------------------------------------------------------------
requests>=2.32.0
aiohttp>=3.9.5
beautifulsoup4>=4.12.3
lxml>=5.2.0
pdfplumber>=0.11.0
pypdf>=4.2.0

# ---------------------------------------------------------------------------
# Database Layer  (src/database)
# ---------------------------------------------------------------------------
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0               # PostgreSQL async driver
psycopg2-binary>=2.9.9        # PostgreSQL sync driver for admin scripts
alembic>=1.13.0               # DB migrations
redis[hiredis]>=5.0.4
qdrant-client>=1.9.0
neo4j>=5.0.0                   # Neo4j async driver for knowledge graph

# Vector / Embedding
sentence-transformers>=3.0.0
numpy>=1.26.4
faiss-cpu>=1.8.0              # optional local vector store fallback

# ---------------------------------------------------------------------------
# Knowledge Ingestion  (scripts/)
# ---------------------------------------------------------------------------
tiktoken>=0.7.0
langchain-text-splitters>=0.2.0
llama-parse>=0.4.0
tqdm>=4.66.4

# ---------------------------------------------------------------------------
# Observability
# ---------------------------------------------------------------------------
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp>=1.24.0
prometheus-client>=0.20.0
structlog>=24.2.0
sentry-sdk[fastapi]>=2.4.0

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest>=8.2.0
pytest-asyncio>=0.23.7
pytest-cov>=5.0.0
httpx>=0.27.0                 # async test client for FastAPI

# ---------------------------------------------------------------------------
# Dev / Linting (install with: pip install -r requirements-dev.txt)
# ---------------------------------------------------------------------------
# ruff>=0.4.7
# mypy>=1.10.0
# pre-commit>=3.7.0
