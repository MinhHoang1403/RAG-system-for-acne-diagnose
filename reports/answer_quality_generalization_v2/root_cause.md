# Answer Quality Generalization V2 Root Cause

| Issue | Expected behavior | Observed failure | First failing stage | Evidence | General fix | Tests added |
|---|---|---|---|---|---|---|
| Structured table/schema completeness | Khi người dùng yêu cầu bảng với số cột, tên cột và entity/hàng cụ thể, answer phải giữ đúng header, số cột và không bỏ entity. | Header có thể thiếu cột, bảng có thể chỉ nói một entity hoặc đảo cột thành hàng. | Prompt/formatter contract và answer verifier. | Verifier cũ chỉ kiểm header sau từ khóa `gồm`, không hiểu `đúng 4 cột:` hoặc rows/entities. | Thêm `RequestedStructure` parser dùng chung; formatter thêm instruction/fallback bảng; verifier kiểm header, exact count, rows và orientation. | `test_requested_table_schema_parser_keeps_columns_rows_and_exact_count`, `test_structured_entity_table_fallback_covers_all_requested_entities`, `test_table_verifier_detects_orientation_inversion_and_missing_rows`. |
| Irrelevant topical warning injection | Warning phải khớp route điều trị: oral isotretinoin/refusal không được chèn cảnh báo giảm tần suất bôi nếu không có topical context. | Answer isotretinoin/refusal có thể chèn cảnh báo topical irritation/frequency không liên quan. | Severity guard caution template và verifier quá hẹp. | Verifier cũ chỉ bắt `isotretinoin đường uống`; caution guard dùng một template topical cho mọi caution. | Chọn caution template theo route/entity; verifier bắt non-topical safety/refusal có topical warning. | `test_irrelevant_topical_warning_is_rejected_for_isotretinoin_refusal_context`. |
| Emergency severity ordering | Với triệu chứng cấp cứu, câu trả lời phải ưu tiên hành động ngay, không mở bằng khuyến nghị chờ 24-48 giờ. | Một số case isotretinoin nghiêm trọng bị xếp urgent và preface 24-48 giờ trước cảnh báo cấp cứu. | Severity classifier/guard. | Rule isotretinoin serious symptoms chỉ trả `urgent_isotretinoin_concerning_symptoms`. | Thêm rule emergency cho isotretinoin + đau đầu dữ dội + nhìn mờ + buồn nôn/nôn; emergency template mở bằng hành động ngay. | `test_isotretinoin_neurologic_symptom_combo_is_emergency_first`. |
| Sign-vs-cause semantic distinction | Nếu hỏi dấu hiệu/triệu chứng, answer phải nêu biểu hiện quan sát được, không thay bằng nguyên nhân/thói quen. | Answer có thể trả lời bằng thói quen/stress/ăn uống cho câu hỏi dấu hiệu. | Prompt contract và verifier. | Không có semantic distinction trong contract/verifier. | Parser nhận `signs_symptoms`; prompt nhắc rõ; formatter có fallback list dấu hiệu; verifier reject cause/behavior drift. | `test_sign_symptom_request_rejects_cause_or_behavior_drift`. |
| Markdown/bold instruction compliance | Nếu yêu cầu tiêu đề đậm, answer phải dùng Markdown bold hợp lệ và frontend render không lộ marker. | Contract chưa nói rõ; verifier không phát hiện thiếu bold. | Prompt contract, formatter, frontend regression. | Chỉ có cleanup Markdown chung, không có requested-style check. | Contract V5 thêm Markdown style request; formatter fallback bold headings; verifier check bold/heading; frontend test render bold headings. | `test_bold_markdown_instruction_is_verifiable_and_render_safe`, `formatText renders requested bold headings without visible markdown markers`. |
| Multi-turn follow-up/coreference | Follow-up như “hoạt chất thứ hai”, “nó”, “vậy tại sao...” phải giữ entity chính và intent. | Generation prompt dùng raw user question; rewrite phụ thuộc LLM và có thể mất entity. | Rewrite node và answer generation node. | `generate_answer_node` dùng `user_question` thay vì `standalone_question`; không có deterministic resolver cho Epiduo/Tazorac/BPO follow-up. | Thêm deterministic coreference resolver từ taxonomy/product ingredients; generation dùng standalone question. | `test_coreference_rewrite_resolves_second_epiduo_ingredient_to_bpo`, `test_coreference_rewrite_preserves_bpo_antimicrobial_followup_intent`, `test_generation_prompt_uses_standalone_question_after_rewrite`. |
| Treatment-summary fallback quality | Với summary điều trị có schema rõ, fallback phải vẫn giữ bảng và safety ordering thay vì một đoạn chung. | Draft ngắn/thiếu có thể lọt qua hoặc không đủ rows/columns. | Formatter fallback. | Không có fallback structured treatment table. | Formatter sinh bảng điều trị tổng quát theo schema người dùng yêu cầu và verifier kiểm lại. | `test_treatment_summary_table_fallback_keeps_rows_for_requested_acne_severity_groups`, `scripts/eval_answer_quality_generalization_v2.py`. |
